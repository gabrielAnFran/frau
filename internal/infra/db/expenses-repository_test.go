package db

import (
	"testing"
)

func TestAddRecurrentExpenses(t *testing.T) {
	// funcUsingEnvVar()///

	// // Create a new repository
	// repo := NewRecurrentExpensesRepository()

	// // Define a test expense
	// expense := models.RecurrentExpense{
	// 	ExpenseName: "Test Expense",
	// 	Amount:      100.0,
	// 	Frequency:   "monthly",
	// 	StartDate:   "2023-01-01",
	// 	EndDate:     "2023-12-31",
	// }

	// // Call the function
	// result, err := repo.AddRecurrentExpenses(expense)

	// // Assertions
	// assert.NoError(t, err)
	// assert.Equal(t, expense.ExpenseName, result.ExpenseName)
	// assert.Equal(t, expense.Amount, result.Amount)
	// assert.Equal(t, expense.Frequency, result.Frequency)
	// assert.Equal(t, expense.StartDate, result.StartDate)
	// assert.Equal(t, expense.EndDate, result.EndDate)
}

func funcUsingEnvVar() {
	// err := godotenv.Load("../../../.env")
	// if err != nil {
	// 	log.Fatal(err)
	// }
}
